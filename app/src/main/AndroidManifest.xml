<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cyl.kaiyan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".utils.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.view.activity.HuancunActivity" />
        <activity android:name=".view.view.activity.JiluActivity" />
        <activity android:name=".view.view.activity.ShowActivity" />
        <activity android:name=".view.view.activity.ShowsActivity" />
        <activity android:name=".view.view.activity.Main2Activity" />
        <activity android:name=".view.view.activity.Main22Activity" />
        <activity android:name=".view.view.activity.FindShowActivity2"></activity>
    </application>

</manifest>